SECRET_KEY=dev-insecure-change-me
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# SQL Server (must meet SQL Server password complexity rules)
MSSQL_SA_PASSWORD=Datapull_Dev_Pass123
DATABASE_URL=mssql+pyodbc://sa:Datapull_Dev_Pass123@localhost:1433/datapull?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

# Bootstrap admin user (consumed by `flask bootstrap-admin`)
ADMIN_EMAIL=admin@datapull.local
ADMIN_PASSWORD=ChangeMe_Admin123

# Fernet key for encrypting secret job parameter values at rest.
# Generate with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECRET_ENCRYPTION_KEY=
